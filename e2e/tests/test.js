const url = 'http://localhost:80';

module.exports = {
  /* 'Home test': browser =>
    browser
      .url(url)
      .waitForElementVisible('body')
      .assert.containsText('h1', 'Anna Liisa')
      .end() */
};
